<?php

declare(strict_types=1);

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| Web Routes
|--------------------------------------------------------------------------
|
| This is an API-only project — no web UI. The /admin-panel static files
| (referenced in the Node backend) will land in P9, served as static assets
| from public/admin-panel/ rather than through Laravel routes.
|
| Keeping this file present (even empty) because bootstrap/app.php declares
| it in withRouting(). Laravel boots without it but logs a warning.
*/
